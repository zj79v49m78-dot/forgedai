{
  "$schema": "../gen/schemas/desktop-schema.json",
  "identifier": "default",
  "description": "Forged's own commands only. No plugin or filesystem capabilities are granted to the webview — every privileged operation goes through a named Rust command that validates its input.",
  "windows": ["main"],
  "permissions": ["core:default"]
}
